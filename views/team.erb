<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
   <% params.each do |k, v| %>
    <% title_name = k.split(/ |\_/).map(&:capitalize).join(" ") %>
    <% if k == "team_name" %>
      <h1><%= title_name %>: <%= v %><h1>
    <% else %>
      <h2><%= title_name %>: <%= v %><h1>
    <% end %>
   <% end %>
 </body>
</html>
